{
  "annotations": {"list": []},
  "editable": false,
  "graphTooltip": 0,
  "panels": [
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"unit": "reqps"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 0},
      "id": 1,
      "title": "Request rate",
      "type": "timeseries",
      "targets": [{"expr": "sum(rate(fastapi_http_requests_total[1m]))", "legendFormat": "requests/sec", "refId": "A"}]
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"unit": "s"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 12, "y": 0},
      "id": 2,
      "title": "P95 request latency",
      "type": "timeseries",
      "targets": [{"expr": "histogram_quantile(0.95, sum(rate(fastapi_http_request_duration_seconds_bucket[5m])) by (le))", "legendFormat": "p95", "refId": "A"}]
    },
    {
      "datasource": {"type": "prometheus", "uid": "prometheus"},
      "fieldConfig": {"defaults": {"unit": "short"}, "overrides": []},
      "gridPos": {"h": 8, "w": 12, "x": 0, "y": 8},
      "id": 3,
      "title": "Responses by status",
      "type": "timeseries",
      "targets": [{"expr": "sum by (status) (rate(fastapi_http_requests_total[5m]))", "legendFormat": "{{status}}", "refId": "A"}]
    }
  ],
  "schemaVersion": 39,
  "style": "dark",
  "tags": ["fastapi", "terraform", "docker"],
  "templating": {"list": []},
  "time": {"from": "now-1h", "to": "now"},
  "title": "FastAPI Overview",
  "uid": "fastapi-overview",
  "version": 1
}
