apiVersion: 1

providers:
  - name: Terraform Docker Lab
    orgId: 1
    folder: Terraform Docker Lab
    type: file
    disableDeletion: true
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
